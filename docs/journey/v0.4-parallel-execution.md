# v0.4.0 - 并行执行

## 当时的问题

互不依赖的工具调用串行执行，会让代码探索和计划任务不必要地变慢。

## 设计选择

只并发执行相互独立的任务，并保持结果顺序稳定。存在依赖关系时仍按 DAG 推进。

## 实现结果

- 三条执行路径统一复用并行工具执行能力。
- 默认并发上限为 4。
- DAG 中同批次独立步骤可并行处理。

## 下一步

让 Agent 可以查阅实时外部信息。
