# v0.1.0 - 三路执行模式

## 当时的问题

单一 Agent 循环适合快速行动，但复杂任务需要计划审阅和角色协作。

## 设计选择

同时保留三条执行路径：默认 ReAct、可审阅的 Plan-and-Execute、面向复杂任务的 Multi-Agent。

## 实现结果

- `Agent` 负责 ReAct。
- `PlanExecuteAgent` 负责计划与 DAG 执行。
- `AgentOrchestrator` 负责多 Agent 协作。

## 下一步

让 Agent 在多轮任务中保留稳定信息。
