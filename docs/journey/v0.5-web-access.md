# v0.5.0 - 联网搜索

## 当时的问题

代码之外的问题经常依赖最新文档、网页正文和外部资料。

## 设计选择

先提供轻量的搜索与抓取能力，再为 SPA、防爬墙和登录态页面准备浏览器 fallback。

## 实现结果

- `web_search` 搜索入口。
- `web_fetch` 抓取已知 URL。
- 网络策略限制不安全目标。
- 浏览器读取优先使用结构化 snapshot。

## 下一步

接入开放工具生态，而不是继续把所有能力写死在仓库里。
