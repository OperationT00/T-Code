# v0.2.0 - Memory

## 当时的问题

长对话会挤压上下文窗口，跨会话的重要信息也会丢失。

## 设计选择

区分短期压缩和长期记忆。长期记忆只在用户明确要求时保存，避免把临时指令误当作稳定事实。

## 实现结果

- 对话历史压缩保护上下文窗口。
- 长期记忆支持项目级和全局作用域。
- `/memory` 与 `/save` 提供审计、搜索和删除入口。

## 下一步

提升 Agent 对大型代码库的理解能力。
