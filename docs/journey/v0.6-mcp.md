# v0.6.0 - MCP

## 当时的问题

内置工具无法覆盖所有外部系统，继续手写集成会快速增加维护成本。

## 设计选择

接入 MCP，支持 stdio 和 Streamable HTTP server，并将动态工具纳入既有 HITL 与审计链。

## 实现结果

- 动态注册 `mcp__{server}__{tool}`。
- 支持 resources 与 prompts。
- 接入 Chrome DevTools MCP。
- MCP 启动不阻塞首屏。

## 下一步

让专业知识也可以按需扩展。
